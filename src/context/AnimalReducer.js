const animalReducer = (state, action) => {
  switch (action.type) {
    case "GET_ANIMALS":
    //   return {
    //     ...state,
    //     animals: action.payload,
    //   };
  }
};

export default animalReducer;
